#include "main.h"
#include <stdio.h>

/**
 * _puts - Calculating length of a given string.
 * @str: string.
 * Return: Non negative number on success, or EOF on error.
 */
void _puts(char *str)
{
puts(str);
}
