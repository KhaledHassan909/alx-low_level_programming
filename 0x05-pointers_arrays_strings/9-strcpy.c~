#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * *_strcpy - function that copies the string pointed to by src to buffer dest.
 * @dest: Buffer pointed to.
 * @src: String source.
 */
char *_strcpy(char *dest, char *src)
{
return (strcpy(dest, src));
}
