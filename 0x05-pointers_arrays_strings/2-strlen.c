#include "main.h"
#include <string.h>

/**
 * _strlen - Calculating length of a given string.
 * @s: string.
 * Return: int length of a string
 */
int _strlen(char *s)
{
int length;           /* intialize integer */
length = strlen(s);  /* length of string s */
return (length);
}
