#include <stdio.h>

/**
* main - Entry point
*
* Description: 'Positive,Negative or Zero'
*
* Return: Always 0 (Success)
*/
int main(void)
{
char ch;
ch = 'a';
while (ch <= 'z')
{
putchar(ch);
ch++;
}
ch = ch - 32;
putchar(ch);
putchar('\n');
return (0);
}
