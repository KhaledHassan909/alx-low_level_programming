#include "main.h"
#include <ctype.h>

/**
 * _islower - checks for lowercase character
 * @c: character to check
 *
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _isupper(int c)
{
if (isupper(c))
return (1);
else
return (0);
}
